# Optional: Perseus seed

<!-- SEEDED ONCE from engineering-standards. Optional seed — delete this
     directory if the host does not run Perseus. Not vendored. Not required
     by a standards sync. -->

Copy these files to `.perseus/` at the repository root **only** if the agent
host has Perseus. Then edit them — they are yours. This is an optional seed,
not required scaffolding for every consumer.

- `context.md` — repo-local briefing template
- `pack.yaml` — render pack; synthesis probes start disabled

Generated output (`.perseus/cache/`, `live-context.md`) stays gitignored.
Conventions live in `standards/`; this directory is recommended wiring, not
a convention.

See `templates/docs/ai/perseus-vault.md`.
